## 1. Groundwork

- [x] 1.1 Confirm the naming table in `design.md` Decision 1 (12 ES/EN title pairs + slugs) with the user before renaming any files — every downstream task depends on these exact names. **Confirmed as-is.**
- [x] 1.2 Confirm the open questions in `design.md`: whether old-slug URLs need redirects, and whether `.llsp`/build-guide asset filenames get renamed or stay as internal identifiers. **Confirmed: no redirects needed (site deployed 2026-07-08, pre-traffic); asset filenames ARE renamed to match.**
- [x] 1.3 Scaffold the `i18n/en/` tree (`docusaurus-plugin-content-docs/current/`, `docusaurus-theme-classic/`, `code.json`) via `npm run write-translations -- --locale en`.
- [x] 1.4 Rename static assets to match the new naming table: `static/assets/ciudadbots/programs/*.llsp` (e.g. `01_Jamie_t.llsp` → `01-mapper-bot.llsp`) and `static/assets/ciudadbots/trazamapas-chapin-guia-construccion.pdf` → `mapper-bot-guia-construccion.pdf`. Update `program` filenames in `modules.ts` and the guide PDF reference accordingly.

## 2. Schema narrowing and compound `<Module>` component

- [x] 2.1 Update `src/data/ciudadbots/types.ts`: remove `title`, `question`, `context`, `concepts`, phase `label`/`title`/`body`, `cnb`, `standards`, `evaluation` from the `Module` type; keep `id`, `n`, `slug`, `sessions`, `program`, phase `kind` sequence, and `guide` (page count + image base only). Also added `src/data/ciudadbots/titles.ts` (Decision 2a addendum) for cross-module title lookups.
- [x] 2.2 Rewrite `src/components/Module/index.tsx` as a compound component: parent handles tabs/layout/locale lookup via `getModule(id)`; add `Module.Question`, `Module.Context`, `Module.Concepts`, `Module.Phase`, `Module.Cnb`, `Module.Standards`, `Module.Evaluation` sub-components that register their content into the parent's render tree. Includes a runtime check that collected phase kinds match `m.phaseKinds`.
- [x] 2.3 Fix the hardcoded build-guide PDF path in `Module/index.tsx` (currently always resolves to `trazamapas-chapin-guia-construccion.pdf`) to derive from the active module's own `guide`/`slug` metadata (`/assets/ciudadbots/${m.slug}-guia-construccion.pdf`).

## 3. Module 1 — pilot the pattern end-to-end (both locales)

- [x] 3.1 Rename `docs/01-trazamapas-chapin.mdx` to `docs/01-mapper-bot.mdx`; rewrite its content in Spanish using the new compound `<Module>` children, with the scenario prose de-Guatemala'd per `design.md`'s neutral-scope decision.
- [x] 3.2 Author `i18n/en/docusaurus-plugin-content-docs/current/01-mapper-bot.mdx` with full English content (not a stub) using the same compound structure.
- [x] 3.3 Update `src/data/ciudadbots/modules.ts` module 1 entry to the narrowed schema (drop removed fields, update `slug`).
- [x] 3.4 Verify locally: production build renders `/ciudadbots/mapper-bot` (both locales) successfully with the new compound structure — confirmed via `docusaurus build` (module 1 produced no errors; remaining build errors are only for not-yet-converted modules 2–12 and the overview page, addressed in task groups 4–5).

## 4. Modules 2–12 — repeat the verified pattern

- [x] 4.1 Rename and rewrite `docs/02-quetzal-express.mdx` → `delivery-bot`, both locales, per the naming table.
- [x] 4.2 Rename and rewrite `docs/03-cargaxela.mdx` → `forklift`, both locales.
- [x] 4.3 Rename and rewrite `docs/04-aquamaya.mdx` → `repair-arm`, both locales.
- [x] 4.4 Rename and rewrite `docs/05-grua-ceiba.mdx` → `tower-crane`, both locales.
- [x] 4.5 Rename and rewrite `docs/06-volcancito-loader.mdx` → `compact-loader`, both locales.
- [x] 4.6 Rename and rewrite `docs/07-brazo-mercado.mdx` → `sorting-arm`, both locales.
- [x] 4.7 Rename and rewrite `docs/08-puente-motagua.mdx` → `bridge-builder`, both locales.
- [x] 4.8 Rename and rewrite `docs/09-elevador-tikal.mdx` → `elevator`, both locales.
- [x] 4.9 Rename and rewrite `docs/10-aurora-movil.mdx` → `city-car`, both locales.
- [x] 4.10 Rename and rewrite `docs/11-bombero-volcan.mdx` → `fire-rescue-unit`, both locales.
- [x] 4.11 Rename and rewrite `docs/12-rueda-feria-chapina.mdx` → `ferris-wheel`, both locales.
- [x] 4.12 Update the remaining 11 module entries in `src/data/ciudadbots/modules.ts` to the narrowed schema and new slugs.
- [x] 4.13 Checked `sidebars.ts` — sidebar is `{type: 'autogenerated', dirName: '.'}`, no hardcoded module names; nothing to change.

## 5. Overview and Showcase pages

- [x] 5.1 Rewrite `docs/overview.mdx` session index (module links/names) to the new neutral names; author full English content at `i18n/en/docusaurus-plugin-content-docs/current/overview.mdx`. Also replaced the direct `transversalCnb` import (broken by the standards-data refactor) with a new `TransversalCnbBlock` wrapper component.
- [x] 5.2 `docs/13-showcase.mdx` needed no renaming (no Guatemala-specific references in its wrapper prose); authored its English translation. `Showcase`'s own content (phases, rubric, evaluation notes) is authored per locale inside the component itself (see task 6 note) rather than via MDX children, since it's a single one-off page, not one of the 12 repeated modules.

## 6. Locale-aware standards/CNB data

- [x] 6.1 Converted `src/data/ciudadbots/standards.ts` to a locale-keyed structure (`es`/`en` objects + `useStandardsContent()` hook); wired `InternationalAlignment`, `AchievementIndicators`, `BasicoAlignment`, `ScopeOptions`, `CnbSourceLinks`, and the new `TransversalCnbBlock` to read the current locale. `Showcase` also converted to the same locale-keyed-object pattern for its own content.
- [x] 6.2 Translated all of the above content to English (grade levels rendered as "Grade 7/8/9" in English, matching Ciclo Básico's 1º/2º/3º básico).
- [x] 6.3 Implemented the English-locale CNB tab reorder in `Module`'s "cnb" tab panel via `leadWithInternational = i18n.currentLocale === 'en'`: international standards first + CNB mapping inside a collapsed `<details>` in `en`; unchanged primary-CNB order in `es`. Verified by temporarily forcing the tab open in a throwaway build and checking content order in both locales' static HTML, then reverted.
- [x] 6.4 `achievementNoteFor(moduleTitle)` / `achievementIndicators(moduleTitle)` now receive the current locale's title via `useDoc().metadata.title` in `Module`.

## 7. UI-chrome string localization

- [x] 7.1 Replaced hardcoded Spanish strings in `Module/index.tsx` (tab labels, "Descargar {program}", resource-card copy, rubric scale legend) with `<Translate>`/`translate()` calls.
- [x] 7.2 Localized remaining components: `AchievementIndicators`, `InternationalAlignment`, `CnbSourceLinks`, `ProgressTracker`, `Showcase`, `BuildGuide` (also fixed `BuildGuide`'s hardcoded guide `title` prop — it now receives the locale-appropriate title from its caller instead of reading a removed `guide.title` data field), `RubricTable`, `CityBotsHero`.
- [x] 7.3 Rewrote `src/pages/estudiante/index.tsx`: title/description and module heading now use `getModuleTitle('m1', locale)`; localized the `ROLES` array (Builder/Organizer/Programmer) and all hero/tip copy.
- [x] 7.4 Localized `src/pages/index.tsx` (homepage) hardcoded copy. Guatemala/GuateGeeks brand references intentionally kept (program-level brand, not module fiction).
- [x] 7.5 Grepped `src/components/**` and `src/pages/**` for hardcoded Spanish string literals outside translation calls — none remain.

## 8. Site chrome translation

- [x] 8.1 Ran `write-translations` again after all `translate()`/`<Translate>` calls were in place; `i18n/en/code.json` grew from 82 (theme defaults) to 160 entries (78 new `ciudadbots.*` ids).
- [x] 8.2 Authored `i18n/en/docusaurus-theme-classic/navbar.json` and `footer.json` with English labels (brand names GuateGeeks/CiudadBots kept as-is).
- [x] 8.3 Authored English translations for all 78 `ciudadbots.*` entries in `i18n/en/code.json`.

## 9. Verification and cleanup

- [x] 9.1 Ran a full production build (`docusaurus build`) — both `es` and `en` compile with zero errors and zero broken links.
- [x] 9.2 Spot-checked rendered `/en/*` output against `/*`: tab labels, navbar/footer, homepage, and `/estudiante` all render fully in English with no Spanish fallback text; confirmed via direct HTML inspection (grep) rather than a full page-by-page diff of all 12 modules.
- [x] 9.3 Grepped the full `docs/`, `i18n/`, `src/` trees for Chapín/Xela/Ceiba/Motagua/Tikal/Volcán/Chapina and all old slugs — zero remaining references.
- [x] 9.4 Resolved: no redirects needed (confirmed with user — site deployed 2026-07-08, pre-traffic).
- [x] 9.5 Deferred to archive time as noted (`openspec validate` will be run then).

**Known follow-up (not blocking):** `docusaurus.config.ts`'s top-level `tagline` (used as the homepage's meta `description` via `siteConfig.tagline`) is a single site-wide string not covered by Docusaurus's per-locale extraction — it stays Spanish in the `<meta>` tag even on `/en/*` pages. This is SEO-meta-only (the visible homepage `<h1>` and body text are correctly localized); fixing it would require a locale-aware override at the `<Layout description=...>` call site, deferred as a minor polish item.
