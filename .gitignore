# Dependencies
/node_modules

# Docusaurus build output & cache
/build
/.docusaurus

# Generated files
.docusaurus
.cache-loader

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

guategeeks-citybuilders*
equipos-educativos/

# WiFi credentials for the Tiempo Circular sketches. Only the .ejemplo template
# is versioned; the real credenciales.h stays on the machine that wrote it, so a
# classroom never publishes its school's WiFi password along with the code.
arduino/**/credenciales.h

